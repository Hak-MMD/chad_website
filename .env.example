# Backend API base URL
# Development:
VITE_API_URL=http://localhost:3001
# Production (set this on your hosting platform, e.g. Render):
# VITE_API_URL=https://your-backend.onrender.com

# Auth bypass for local UI development (no server needed)
# Set to false or remove entirely before deploying
VITE_BYPASS_AUTH=true
